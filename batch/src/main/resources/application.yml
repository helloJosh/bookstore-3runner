server:
  port: 8084

spring:
  profiles:
    active: local

  application:
    name: 3runner-batch
  jpa:
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://admin:1234@localhost:8761/eureka

feign:
  coupon:
    url: localhost:8080